#include <stdio.h>

int main()
{
    int num;
    int sum=0;
    
    while(1)
    {
        scanf("%d",&num);
        if(num<=0)
        {
            break;
        }
        if(num%2==1)
        {
            sum+=num;
        }
    }
    
    printf("sum=%d",sum);
    
    return 0;
}